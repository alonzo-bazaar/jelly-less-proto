package lang;

public class LispList {
    /* lists are usually conses
     * though empty lists are nil
     * this makes for a rather unfortunate mess of nil/non nil checks in the code
     * to obviate this, we shall use this helper class to manipulate an internal
     * list expression, be it nil or cons, and extract it return once
     * the manipualtion is done, thus freeing the rest of the code from these checks
     */
    // TODO the class is empty
}
