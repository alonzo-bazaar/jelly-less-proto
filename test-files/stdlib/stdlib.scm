;; todo, handle loading file from file
;; all relative filepaths are currently considered from the cwd of the running process
;; change that plz? (set a cwd in the runtime and use that to evaluate relative paths?)
